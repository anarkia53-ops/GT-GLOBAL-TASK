import { PlaceholderPage } from '@/components/shared/PlaceholderPage'

export default function Page() {
  return (
    <PlaceholderPage
      title="/app/billing"
      description="Client platform route placeholder — backend services (Auth/DB/RBAC/OpenAI) are scaffolded for future activation."
    />
  )
}
